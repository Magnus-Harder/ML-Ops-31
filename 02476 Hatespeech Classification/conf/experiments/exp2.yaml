# Experiment 1: parameters for the "fast" model
hyperparameters:
  model_type: Base
  hidden_dim: 128
  activation: relu
  dropout:    0.2

training_parameters:
    batch_size: 32
    learning_rate: 0.001
    epochs: 10
    optimizer: adam 